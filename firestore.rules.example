rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    match /planificadores/{document=**} {
      allow read, write: if signedIn();
    }
  }
}

